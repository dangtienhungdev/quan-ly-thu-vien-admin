// import { useRouterState } from '@tanstack/react-router';
import LoadingBar from 'react-top-loading-bar';

export function NavigationProgress() {
	return <LoadingBar color="#6366f1" progress={0} />;
}
